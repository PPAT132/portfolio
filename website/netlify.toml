# Netlify Configuration for Portfolio Website

[build]
  publish = "dist"
  command = "npm run build"

# SPA routing - redirect all routes to index.html
# Note: This must be the LAST redirect rule
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

